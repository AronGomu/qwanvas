# Use HTML/CSS project files for AI interchange

Qwanvas will not embed an in-app AI prompt workflow; AI tools operate outside the app by generating or editing portable HTML/CSS project files that the app can import. The HTML file includes both readable rendered markup and embedded editable project data, trading a slightly more verbose file format for portability, inspectability, and compatibility with external AI/tooling workflows.
