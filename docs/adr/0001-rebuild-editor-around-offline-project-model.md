# Rebuild the editor around an offline project model

The existing carousel generator proves the editing concept, but it is centered on Markdown specs, generated HTML, and a local Node watch server. Qwanvas will reuse the vocabulary and useful interaction ideas from that prototype, but rebuild the product as a browser-only offline-first application with a structured project model so projects, pages, elements, undo history, and exports work without a backend or development server.
